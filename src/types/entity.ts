// ============================================================
// 实体基类（所有数据表的公共字段）
// ============================================================

/** 所有实体共享的时间戳字段 */
export interface Entity {
  id: string; // UUID v4
  created_at: string; // ISO 8601
  updated_at: string; // ISO 8601
}
